{ config, pkgs, ... }:

{
  # =========================================================================
  # VMware 虚拟机 guest 支持
  # 物理机部署时删除此 import 或替换为对应虚拟化配置（Docker/libvirt 等）
  # =========================================================================
  virtualisation.vmware.guest.enable = true;
}
